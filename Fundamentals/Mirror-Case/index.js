function mirror(string) {
    // You will be given a string with mixed uppercase and lowercase letters.
    // You must determine if each character is uppercase or lowercase, and return a string with the opposite casing.
    // Example: an input of "HeLLo wOrLD" will return "hEllO WoRld"

    return null;
}

module.exports = mirror;
