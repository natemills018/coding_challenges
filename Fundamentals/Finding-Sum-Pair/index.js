function findPair(arr, target) {
    // Given an array of positive integers, find if any combination of two equal the target number.
    // Return true if a combination can be found, false if there is no pair summing up to the target number

    // findPair([1, 2, 4, 6, 7], 9001) === false
    // findPair([1, 2, 4, 6, 7], 9) === true, since 2+7 === 9

    return null;
}

module.exports = findPair;
