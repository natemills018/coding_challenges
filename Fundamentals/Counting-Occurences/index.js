function countOccurrences(str) {
    // In this challenge, you will be receiving a word and you must count the frequency of each letter
    // You should return an object with the keys being the letter, and the count being the respective values
    // (sidenote - you may hear this data structure referred to in other challenges as a histogram)
    // Ex: countOccurrences("gabagool") would return { g: 2, a: 2, b: 1, o: 2, l: 1 }

    return null;
}

module.exports = countOccurrences;
