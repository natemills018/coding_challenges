function isPalindrome(string) {
    // Your function here should return just a boolean:
    // return `true` if the input string is the same reversed as it is normally
    // return `false` if it is not a palindrome.

    return string;
}

module.exports = isPalindrome;
