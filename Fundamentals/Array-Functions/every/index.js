function every(arr) {
    // You are given an array of a few different strings.
    // You must return a boolean on whether or not that array has every string being at least 9 characters or more in length
    // For example, if arr === ["restaurant", "spaghetti", "antidisestablishmentarianism", "cat"]
    //    arr.every((word) => word.length >= 9) would return `false` since not all strings match that condition.
    // Instead of using .every, you must use just a regular for loop.

    let results = null;

    // Define your for loop here
    // return true or false based on whether or not every string in the array is 9 or more characters

    return results;
}

module.exports = every;
